{
  "name": "macau-sync-v13",
  "version": "13.0.0",
  "description": "澳门洗码报表 v13 — Event Bus + State Manager 全新架构",
  "main": "server.js",
  "scripts": {
    "build": "node build.js",
    "start": "node build.js && node server.js",
    "test": "node build.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
